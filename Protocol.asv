function res = Protocol()
    protocol = getProtocol();
    res = protocol;
end

function protocol = getProtocol()
    users = ['user1', 'user2'];
    code = [1, -1];
    
    protocol = containers.Map;
    protocol(users(1)) = code;
    protocol(users(2)) = -1.*code;
end